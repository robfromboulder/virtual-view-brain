# Test View Renaming: Manual Test Plan

Checks run by hand for this work item (UI, API calls, data checks). At closeout these fold into the canonical testing doc.

## Setup

No special setup — uses the existing ViewZoo test infrastructure.

## Test cases

1. **Run full test suite** — verify all new rename test cases pass alongside existing tests.
   - **Expected:** All tests green, no regressions